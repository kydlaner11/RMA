## ESLint

### Project Setup

Follow this instruction to setup eslint in this project

- Run `npm install eslint eslint-plugin-react eslint-plugin-react-hooks --save-dev`
- Create `.eslintrc.json` file in root of folder
- Copy this project `.eslintrc.json` to your project, then modified it as you need
